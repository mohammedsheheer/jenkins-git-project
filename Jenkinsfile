pipeline {
    agent any
    stages {
        stage('Build') { steps { echo 'This is Build Stage' } }
        stage('Test') { steps { echo 'Test' } }
        stage('Deploy'){ steps { echo 'Deploy' } }
    }
}
